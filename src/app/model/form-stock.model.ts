export class FormStock {





    constructor(public id?: number, public name?: string, public code?: string, public price?: number, public previousPrice?: number) {
        id = id;
        name = name;
        code = code;
        price = price;
        previousPrice = previousPrice;
    }
}

